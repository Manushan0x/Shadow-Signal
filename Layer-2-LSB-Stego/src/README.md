How to Run it Correctly
If you are using a terminal or command prompt:

Compile everything first:
javac src/*.java

Run the Receiver (to decode):
java -cp src ReceiverMain

Run the Sender (to encode):
java -cp src SenderMain